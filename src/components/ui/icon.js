import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Icon = props => {
  const {name} = props;
  return (
    <View>
      <Text>icon</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Icon;
